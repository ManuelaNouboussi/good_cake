export class Category {
    constructor({
      id,
      name,
      slug,
      description,
      createdAt
    }) {
      this.id = id;
      this.name = name;
      this.slug = slug;
      this.description = description;
      this.createdAt = createdAt;
    }
  }
  